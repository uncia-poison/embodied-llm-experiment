name: disconnected-control
seed: 2026
ticks: 240
output_dir: runs
body:
  action_dim: 8
  dt: 0.2
world:
  autonomous: true
  event_probability: 0.05
sensorium:
  mode: permuted
  channel_seed: 7719
  decimals: 3
drive:
  mode: semantic
  action_dim: 8
  embedding_provider: sentence_transformers
  embedding_model: intfloat/multilingual-e5-small
memory:
  mode: full
model:
  provider: ollama
  model: qwen3:8b
  endpoint: http://localhost:11434/api/chat
probes:
  enabled: true
  warmup_ticks: 80
  agency_every: 12
  prediction_every: 12
coupling:
  - start: 0
    end: 240
    mode: random

prompt:
  profile: neutral
